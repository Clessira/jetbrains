# Local/dev fallback only — releases are versioned from the pushed git tag
# via PLUGIN_VERSION (see .github/workflows/release.yml).
pluginVersion=0.1.1
platformVersion=2024.2.5

# The Kotlin stdlib ships with the IntelliJ Platform — never bundle our own copy.
kotlin.stdlib.default.dependency=false
kotlin.code.style=official

org.gradle.caching=true
org.gradle.jvmargs=-Xmx2g
